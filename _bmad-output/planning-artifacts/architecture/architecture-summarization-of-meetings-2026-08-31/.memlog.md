---
scope: Вся система локальной суммаризации встреч
purpose: Краткий build substrate для согласованной реализации людьми и агентами; дополнительных материалов не требуется
altitude: initiative
updated: 2026-09-01T14:06
---

- (direction) Выбран Fast path: архитектурный spine готовится целиком, а незафиксированные решения получают тег [ASSUMPTION].
- (constraint) Единственный требуемый результат — краткий ARCHITECTURE-SPINE.md; дополнительные презентационные или пояснительные материалы не нужны.
- (constraint) Финальный PRD и техническое приложение от 2026-08-31 являются входными источниками; утверждённые продуктовые решения наследуются без повторного согласования.
- (constraint) Вся система — локальное однопользовательское web-приложение без авторизации; штатная публикация только на loopback; поставка через Docker Compose на Linux, macOS и Windows.
- (constraint) SQLite владеет постоянными метаданными, расшифровками, саммари, настройками и заданиями; крупные медиафайлы принадлежат управляемой локальной файловой области; автоматический fallback из локального режима во внешний запрещён.
- (constraint) Транскрибация и суммаризация выбираются независимо между локальным и OpenAI-совместимым движком; каждый вывод саммари о решении, задаче или важном моменте обязан иметь ссылку на сегмент либо статус отсутствия подтверждения.
- (direction) Для UI рассмотрены серверные шаблоны, full-stack TypeScript плюс Python ML worker и React SPA плюс Python API. Выбран последний вариант: интерактивный редактор и живой прогресс остаются в React, а медиа и ML — в Python; решение помечается [ASSUMPTION].
- (direction) Для формы системы рассмотрены микросервисы, обычный слоёный монолит и гексагональный модульный монолит с раздельными процессами исполнения. Выбран третий вариант: он сохраняет простую поставку и фиксирует границы движков и приватности; решение помечается [ASSUMPTION].
- (direction) Для фоновой обработки рассмотрены in-process задачи, Redis/Celery и SQLite-backed durable state machine. Выбрана SQLite state machine: in-process не переживает сбой, а Redis/Celery добавляет лишнюю инфраструктуру для одного пользователя; решение помечается [ASSUMPTION].
- (direction) Для локального AI рассмотрены Ollama, прямые Transformers и faster-whisper плюс llama.cpp. Выбран воспроизводимый CPU-baseline faster-whisper и llama.cpp внутри Compose; конкретные модели остаются [ASSUMPTION] до проверки на корпусе выпуска.
- (decision) AD-1 [ASSUMPTION] — Binds: all. Prevents: циклы между возможностями и проникновение SQLite, файлов, HTTP и SDK провайдеров в доменную логику. Rule: гексагональный модульный монолит; entrypoints зависят от application, application — от domain и ports, adapters реализуют ports; модули общаются только через публичные application-команды и запросы.
- (decision) AD-2 [ADOPTED] — Binds: FR-1–4, FR-10–18, NFR-4. Prevents: два владельца данных, битые ссылки и частично опубликованные результаты. Rule: SQLite — источник истины для структурированного состояния, управляемая файловая область — для media/artifacts/models; в БД только относительные идентификаторы, размер и checksum; файл сначала пишется в staging и атомарно публикуется, удаление идёт через восстанавливаемый статус deleting.
- (decision) AD-3 [ADOPTED] — Binds: FR-13–15, NFR-2–5. Prevents: дубли обработки, потерю прогресса и повтор уже успешных этапов. Rule: ProcessingJob — сохраняемая state machine prepare→transcribe→summarize; не более одного активного job на Meeting; worker арендует StageAttempt с heartbeat, вычисляет input fingerprint, коммитит output и checkpoint атомарно, а retry создаёт новую попытку только требуемого этапа; cancel кооперативен и проверяется на безопасных точках.
- (decision) AD-4 [ADOPTED] — Binds: FR-5–10, FR-16–18. Prevents: ложную взаимозаменяемость ASR и LLM и скрытый fallback. Rule: независимые TranscriptionEngine и SummarizationEngine ports имеют типизированные request/result/error и capability manifest; профиль сохраняет capability snapshot; adapter нормализует provider-ошибки; выбор движка фиксируется на попытку, автоматическая замена движка запрещена.
- (decision) AD-5 [ASSUMPTION] — Binds: FR-6–7, SM-5, NFR-1, NFR-7–8. Prevents: исходящий трафик из локального этапа и случайную публикацию приложения в LAN. Rule: Compose содержит app, local-worker, provider-worker, llama-server и одноразовый model-fetch; host port app привязан к 127.0.0.1 и ::1; local-worker и llama-server имеют только internal network без default route; provider-worker — единственный процесс обработки с egress; model-fetch не монтирует данные встреч.
- (decision) AD-6 [ASSUMPTION] — Binds: FR-5, FR-7, NFR-1, NFR-5. Prevents: сохранение или утечку API-ключей через БД, jobs, export и logs. Rule: БД хранит только secret_ref; ключ поступает из process environment либо текущей UI-сессии, остаётся в memory-only credential broker и выдаётся provider-worker только на время StageAttempt по приватному каналу; restart инвалидирует session secret и переводит попытку в waiting_for_secret.
- (decision) AD-7 [ADOPTED] — Binds: FR-10–12, FR-16–18, FR-20–21, SM-3–4. Prevents: саммари, которое нельзя проверить после редактирования. Rule: Transcript имеет monotonically increasing revision; EvidenceRef хранит segment_id и start_ms/end_ms; Summary хранит source_transcript_revision, engine snapshot, generated_at и manual_edit; изменение сегментов повышает revision и делает Summary stale; invalid/missing evidence становится unverified и сохраняется в UI и экспорте.
- (decision) AD-8 [ASSUMPTION] — Binds: FR-16–18, SM-4. Prevents: несовместимые свободные ответы моделей и потерю provenance при длинных встречах. Rule: summarization pipeline передаёт модели сегменты со стабильными IDs, извлекает типизированные candidates по token-budget chunks, сводит только candidates с их evidence IDs и валидирует итог доменной JSON Schema; только валидатор может записать Summary.
- (decision) AD-9 [ASSUMPTION] — Binds: FR-3, FR-19, NFR-1–2. Prevents: внешний поиск, расходящиеся индексы и отдельную поисковую инфраструктуру. Rule: SQLite FTS5 индексирует текущие тексты Segment и Summary в той же транзакции, что и их публикация; MVP использует lexical search без embeddings; результат всегда возвращает Meeting, тип совпадения и локальный snippet.
- (decision) AD-10 [ASSUMPTION] — Binds: FR-2, FR-4, FR-14, NFR-4, NFR-8. Prevents: несовместимые схемы после обновления и backup без соответствующих media. Rule: Alembic имеет один линейный forward-only head; migration gate до запуска сервисов делает pre-upgrade backup; backup в maintenance mode содержит SQLite online snapshot, media и versioned manifest, исключает model cache; restore при остановленных сервисах валидирует manifest и checksums до замены данных.
- (decision) AD-11 [ASSUMPTION] — Binds: all UI/API capabilities, NFR-2, NFR-5–6. Prevents: расхождение frontend/backend контрактов и нестабильные форматы. Rule: REST JSON API под /api/v1 и его OpenAPI — единственный источник frontend DTO/client; прогресс идёт через resumable SSE, состояние сервера авторитетно; IDs — UUIDv7, время — RFC 3339 UTC, media offsets — integer milliseconds; ошибки — RFC 9457 Problem Details со stable code, stage и retryable.
- (decision) AD-12 [ASSUMPTION] — Binds: FR-6, FR-9–10, FR-16–17, NFR-2–3, SM-2–5. Prevents: одновременную загрузку моделей сверх 16 ГБ и неповторяемое качество. Rule: local-worker выполняет только один compute-heavy StageAttempt, ASR и LLM загружаются последовательно; CPU/int8 — обязательный baseline, accelerator — опциональный Compose profile; release manifest pin-ит model revision и parameters; модели допускаются только после corpus gates. Начальные кандидаты: Whisper large-v3-turbo CT2 и Qwen3-4B-GGUF Q4_K_M.
- (version) Python 3.13.15 выбран вместо latest 3.14.7 из-за неподтверждённой стабильным faster-whisper 1.2.1 совместимости с 3.14; официальная версия Python: https://www.python.org/downloads/windows/ ; faster-whisper requirements/release: https://github.com/SYSTRAN/faster-whisper и https://github.com/SYSTRAN/faster-whisper/releases/tag/v1.2.1
- (version) Backend seed verified 2026-08-31: FastAPI 0.141.1 https://github.com/fastapi/fastapi/releases/tag/0.141.1 ; SQLAlchemy 2.0.52 https://github.com/sqlalchemy/sqlalchemy/releases/tag/rel_2_0_52 ; Alembic 1.19.1 https://github.com/sqlalchemy/alembic/releases/tag/rel_1_19_1 ; SQLite 3.53.4 https://www.sqlite.org/releaselog/3_53_4.html ; FFmpeg 9.0.1 https://ffmpeg.org/download.html
- (version) Frontend seed verified 2026-08-31: React 19.2.7 https://react.dev/versions ; Vite 8.2.2 https://github.com/vitejs/vite/releases/tag/v8.2.2 ; TypeScript 6.0.3 https://github.com/microsoft/TypeScript/releases/tag/v6.0.3 ; Node.js build runtime 24.20.0 LTS https://nodejs.org/en/download
- (version) Local AI seed verified 2026-08-31: faster-whisper 1.2.1; llama.cpp v0.3.0 https://github.com/ggml-org/llama.cpp/releases/tag/v0.3.0 ; Whisper CT2 dropbox-dash/faster-whisper-large-v3-turbo@0a363e9 MIT https://huggingface.co/dropbox-dash/faster-whisper-large-v3-turbo ; Qwen/Qwen3-4B-GGUF Q4_K_M@bc640142c66e1fdd12af0bd68f40445458f3869b Apache-2.0 https://huggingface.co/Qwen/Qwen3-4B-GGUF
- (version) Deployment seed verified 2026-08-31: Docker Compose 5.4.0 bundled with Docker Desktop 4.87.0 and newer; official evidence https://docs.docker.com/desktop/release-notes/ . Images and model snapshots must be pinned by digest/revision in release manifest.
- (assumption) Весь выбранный stack, paradigm и новые AD, не утверждённые PRD, приняты по Fast path и будут отмечены [ASSUMPTION] в spine.
- (assumption) Whisper large-v3-turbo CT2 и Qwen3-4B-GGUF Q4_K_M — кандидаты, а не доказанно прошедший baseline; пересмотр обязателен до завершения local AI adapter после прогона корпуса выпуска на CPU-машине с 16 ГБ RAM и проверки лицензий transitive artifacts.
- (question) Неблокирующий для scaffold вопрос: пройдут ли кандидаты локальных моделей пороги WER/precision/recall и UX по времени на целевой 16-ГБ CPU-машине? Revisit: до признания local processing feature complete.
- (decision) AD-4 уточнён после reconciliation: ProcessingProfile состоит из независимых TranscriptionStageConfig и SummarizationStageConfig; каждая фиксирует engine_kind, endpoint/base_url, model_id, capability snapshot и optional secretRef. Все четыре комбинации local/provider обязаны работать; ProcessingJob хранит неизменяемый snapshot обеих конфигураций.
- (decision) AD-5/AD-6 уточнены после reconciliation: provider-worker не монтирует SQLite, meeting-data или model volume и получает от app только immutable stage-scoped envelope по аутентифицированному внутреннему RPC; allowlist transcription = prepared audio stream + language/model/config, summarization = current transcript chunks + instructions/model/config. Consent proof и profile snapshot фиксируются до dispatch. SecretProvider выдаёт single-use opaque attempt grant с TTL не дольше lease; ключ запрещён в Compose YAML/env-file, DB, job/event, CLI args, durable queue и logs; grant отзывается при completion/cancel/restart.
- (decision) AD-13 [ASSUMPTION] добавлен после reconciliation — единая intake command стримит media в staging, проверяет ffprobe container/audio track/decodability и продуктовые лимиты, затем атомарно создаёт MediaArtifact и Meeting; ProcessingJob не существует до успешной проверки, rejection очищает staging.
- (decision) AD-14 [ASSUMPTION] добавлен после reconciliation — release gate обязан пройти Linux/macOS/Windows на 16-GB CPU host, corpus quality/resource gates, license/redistribution review и сформировать Apache-2.0 NOTICE, SBOM и manifest с runtime/model/source/checksum/license; провал кандидата приводит к замене за AD-4 port.
- (decision) AD-15 [ASSUMPTION] добавлен после reconciliation — API/UI изолированы от compute, импорт и provider upload стриминговые, запросы bounded/paginated; release benchmarks проверяют library/navigation ≤2 s для 1000 meetings, p95 UI/API ≤1 s во время local job, cancel safe point ≤10 s и persisted progress/heartbeat ≤5 s.
- (decision) AD-3/AD-7/AD-9 уточнены после reconciliation: успешная retry-попытка сохраняет только audit metadata старых попыток, payload/checkpoints очищаются; manual summary replacement требует явного подтверждения и revision precondition; FTS5 индексирует title, current segments и current summary, имеет bounded pagination и детерминированный rebuild/repair из authoritative tables; search benchmark выполняется под compute load.
- (decision) Final Reviewer Gate: AD-2/AD-13 получают crash-consistent artifact saga. staging и final обязаны быть на одном filesystem; DB сначала фиксирует pending, idempotent rename публикует файл, CAS переводит artifact в ready; reads видят только ready; startup reconciliation завершает либо удаляет pending/orphans.
- (decision) Final Reviewer Gate: AD-3 получает lease fencing. Каждая аренда повышает leaseEpoch; heartbeat/checkpoint/completion используют CAS по attemptId+leaseEpoch+running. Stage становится succeeded в одной SQLite-транзакции с authoritative output и FTS. Provider dispatch имеет stable dispatchId и идемпотентен.
- (decision) Final Reviewer Gate: AD-4 immutable job snapshot включает обе resolved stage configs, languages, instructions/template revision, parameters, capability/adapter versions, endpoint identity, model revision/checksum; secret value исключён. Capability проверяется при сохранении profile и старте job; evidence flow требует timestamped segments.
- (decision) Final Reviewer Gate: AD-5/AD-6 provider RPC использует единый versioned generated wire contract с TLS, workload authentication и совместимостью. Envelope содержит dispatchId, attemptId, leaseEpoch, stage, input fingerprint, snapshot digest, consent proof и opaque grant; grant redeem-ится один раз через broker, secret живёт только в памяти attempt.
- (decision) Final Reviewer Gate: AD-7/AD-8 validation требует уникальные segment IDs, 0≤startMs<endMs≤mediaDuration, стабильный порядок и отсутствие chunk duplicates; EvidenceRef обязан разрешаться в segment той же transcript revision; невалидный output не завершает stage.
- (decision) Final Reviewer Gate: AD-10 migration/backup/restore использует persisted exclusive maintenance lock, drain workers, checked complete manifest и generation protocol. Migration стартует только после verified complete backup; failure не запускает services. Restore проверяется в новой generation и активируется одним recoverable switch.
- (decision) Final Reviewer Gate: AD-11 SSE cursor — persisted monotonic revision per job; events — replaceable state snapshots. Reconnect отдаёт revisions after cursor или current resync при gap/pruning; клиент игнорирует stale revisions; terminal state всегда доступен REST.
- (decision) Final Reviewer Gate: AD-12 вводит единого LocalResourceCoordinator с exclusive model-residency lease; local-worker запускает pinned llama-server как child только после подтверждённой выгрузки ASR и завершает после summary; peak RSS измеряется для всего Compose. Python sqlite3 обязан быть связан с SQLite 3.53.4+FTS5; known network filesystems запрещены. FFmpeg/PyAV provenance, build flags/codecs/checksums входят в release manifest.
- (decision) Final Reviewer Gate: AD-14 release gate также проверяет NFR-6 keyboard/labels/non-color status в Chromium/Firefox, NFR-10 one-screen happy path и SM-6 clean-host Compose install ≤20 min без model downloads на Linux/macOS/Windows.
- (assumption) Finalize triage: blockers закрываются правилами AD-2–15. Revisit gates: AD-1 dependency test before first module merge; AD-5/6 threat/data-flow and secret-restart tests before provider flow; AD-8 schema/corpus fixtures before summarization; AD-9 1000-meeting benchmark under load before search complete; AD-10 restore drill before first migration; AD-11 OpenAPI/SSE reconnect tests before frontend integration; AD-12 corpus/peak-RSS before local processing complete; AD-13 crash injection before intake release; AD-14/15 OS/license/SBOM/performance gates before first public release.
- (decision) Final Reviewer Gate recheck: AD-5 требует HTTPS с полной certificate/hostname validation для любого non-loopback provider origin. consentProof = acceptedAt + policyVersion + digest(stage, profileRevision, endpointOrigin, provider/model identity, dataCategories, snapshotDigest); любое изменение входа инвалидирует proof и требует нового явного подтверждения.
- (event) Assumption triage завершён: блокирующих допущений нет. Все [ASSUMPTION] остаются допустимыми Fast-path defaults и имеют явные revisit gates в Deferred; смена правила выполняется поправкой стабильного AD ID.
- (event) spine finalized
- (assumption) Update 2026-09-01 выполняется по Fast path; поскольку дополнительный human-facing материал не запрошен, единственный deliverable остаётся ARCHITECTURE-SPINE.md.
- (constraint) Актуальный PRD с FR-1–FR-24 и финальные DESIGN.md/EXPERIENCE.md от 2026-09-01 являются входами update; прямой импорт audio/video, бессрочное хранение source media, structured exact/prefix taskNumber search и Ant Design наследуются как утверждённые решения.
- (version) Ant Design frontend seed verified 2026-09-01: antd 6.6.2, latest official GitHub release 2026-08-28 https://github.com/ant-design/ant-design/releases/tag/6.6.2 ; v6 supports React >=18 and React 19 without compatibility patch https://ant.design/docs/react/migration-v6/ ; theming uses ConfigProvider and design/component tokens https://ant.design/docs/react/customize-theme/ .
- (decision) AD-2 и AD-13 уточняются для direct audio: MediaArtifact различает role source и preparedAudio, а source хранит mediaKind audio|video, исходное имя/container/codec/duration и оригинальные bytes в managed storage без TTL до explicit delete. Intake принимает ровно один audio/video source одним streaming/crash-consistent путём; prepare создаёт отдельный canonical preparedAudio для обоих mediaKind, не изменяя source. Поддерживаемые audio formats/limits остаются OQ-1 release gate.
- (decision) AD-9 и AD-11 уточняются для taskNumber: Meeting хранит display taskNumber и детерминированный taskNumberNorm; один non-unique partial B-tree index ix_meeting_task_number_norm(task_number_norm, id) обслуживает equality и prefix range вне FTS5. GET /api/v1/meetings принимает taskNumber вместе с обязательным taskNumberMatch=exact|prefix, комбинирует structured filter с прочими facets, возвращает matchedField=taskNumber и matchKind, а no-hit — HTTP 200 с пустой page и echo query без fuzzy/semantic fallback. Normalization одинакова на write/query: Unicode NFKC, trim и Unicode casefold; prefix bound вычисляется application layer для SQLite BINARY order.
- (decision) AD-16 [ADOPTED] добавляется для frontend design system — Binds: all frontend UI, final UX, NFR-6. Prevents: параллельные component systems, расходящиеся theme/accessibility semantics и fragile overrides внутренних DOM-структур. Rule: production UI использует antd components и штатный API Ant Design 6; один frontend composition root владеет ConfigProvider/App, русской locale, system light/dark algorithms и design/component tokens. Product code сначала использует component props и composition; custom CSS допускается только для layout/product-specific delta, не воспроизводит доступный antd component и не обращается к internal DOM/class selectors. Product accessibility contracts и tests остаются обязательными поверх library defaults.
- (decision) Reconcile final UX: AD-16 уточнён по более строгому DESIGN.md — CSS, CSS Modules, styled wrappers, visual styles/classNames hooks и internal selectors запрещены; browser-native video/audio controls являются единственным исключением, fallback controls собираются из antd. AD-11 фиксирует streaming source-media GET/HEAD и single HTTP byte-range contract для seek без раскрытия filesystem path. AD-14 gate проверяет WCAG 2.2 AA outcomes финального UX.
- (decision) Reconcile final PRD: AD-2 разрешает media-only deletion только после успешно завершённой обработки с опубликованными Transcript/Summary. AD-8 делает успешную summarization единственным атомарным владельцем публикации Summary и auto tags: failed stage tags не меняет, regeneration заменяет только прежние auto tags и сохраняет manual tags.
- (event) Reconciliation актуального PRD/addendum и финальных UX spines завершён; high findings по delete precondition и CSS policy исправлены, media playback и auto-tag ownership сделаны явными.
- (decision) Reviewer Gate media/data fix: AD-2/AD-13 define source, canonical asr-audio-v1 WAV PCM s16le mono 16 kHz, and playbackArtifact; source is reused only when browser allowlisted, otherwise playback-v1 creates audio WebM/Opus or video WebM/VP9+Opus. Derived identity is keyed by source checksum/role/profile, derivatives persist until explicit media/full deletion, and release gate proves every supported format plays and seeks in Chromium/Firefox.
- (decision) Reviewer Gate concurrency fix: Meeting lifecycleEpoch/DeleteIntent fences media/full deletion from jobs, browser readers and provider streams; persisted leases drain before files/rows. AD-3 completion CAS includes lifecycle/input and Transcript/Summary/Tag revisions; stale output becomes superseded. Retry keeps immutable job snapshot, while changed inputs or regeneration create a new job that may reuse valid upstream outputs. AD-5 app-owned SQLite ProviderDispatch ledger and ACK→persist inFlight→commit handshake make same-dispatch replay cached or outcomeUnknown without repeated egress.
- (decision) Reviewer Gate API/UI fix: task_number remains display TEXT while normalizer-v1 UTF-8 bytes use BLOB index and canonical prefix upper=p+0xFF, preserving leading zeros and Unicode equality/prefix behavior. Playback API moved to ready playback artifact and fixes GET/HEAD/single-Range status/header matrix. AD-16 fixes ConfigProvider→App nesting, context-bound feedback hooks and a blocking no-CSS feasibility spike before first frontend feature merge.
- (event) Configured Reviewer Gate findings applied: playback compatibility, preparedAudio lifecycle, deletion/read fencing, late-output fencing, provider dispatch ownership, taskNumber affinity/prefix bound, Range semantics and Ant Design context/feasibility.
- (decision) Adversarial recheck fix: derived artifact identity/uniqueness теперь включает meetingId и sourceArtifactId, поэтому одинаковые source bytes разных Meetings никогда не создают shared deletion ownership.
- (decision) Adversarial recheck fix: lifecycleState различает mediaDeleting и deleting; DeleteIntent имеет scope media|meeting, artifact leases включают artifactId/owner/epoch/expiry с heartbeat и chunk fencing, а отдельные final transactions либо возвращают Meeting active без media, либо удаляют весь tombstone graph.
- (decision) Adversarial recheck fix: ProviderDispatch ACK возвращает authenticated worker session nonce; app сохраняет inFlight только CAS по current attempt/lease/lifecycle epochs и выдаёт single-use commit token по той же RPC session, поэтому другой/restarted worker не может совершить egress.
- (event) Reviewer Gate final recheck: rubric, version-fit и adversarial lenses имеют 0 critical/high findings; deterministic lint имеет 0 findings.
- (event) Medium/low triage завершён: media representation identity и multi-facet URL/API contract закреплены в AD-11; participant, model-fetch, delete visibility, NFR mapping и UX stale-note follow-ups отложены с явными revisit conditions в Deferred/review files.
- (event) spine finalized
